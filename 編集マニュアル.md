# LABO 70周年サイト 編集マニュアル

このサイトは、**ブラウザだけで**文章や写真を自分で直せるようになっています。
**文章の書き換えはパソコンの Google Chrome で行ってください。** スマホのブラウザや、他のアプリの中で開いたページでは、GitHubの編集画面に文字が打てないことがあります（写真の差し替えだけならスマホでもできます）。

やることは、いつも同じ3ステップです。

1. **GitHub（ギットハブ）** という「サイトのファイル置き場」を開く
2. 文字を書き換える／写真を入れ替える
3. 緑のボタンで保存する → **1〜2分で自動的にサイトに反映**される（サイトのURL：**https://labo-70th.pages.dev/**）

> 用語メモ
> - **GitHub**：サイトのファイルを預けている場所。無料。ここのファイルを変えると、サイトが自動で更新されます。
> - **リポジトリ**：GitHubの中の「このサイト専用のフォルダ」のこと。
> - **コミット（Commit）**：「保存する」のこと。GitHubでは保存ボタンに Commit と書いてあります。

---

## 1. GitHubにログインして、サイトのフォルダを開く

1. ブラウザで `https://github.com` を開き、右上の **Sign in** からログインします。
2. ログインしたら、サイトのフォルダ（リポジトリ）を開きます。
   - 直接開く場合のURL：**https://github.com/hairlabo1956-cell/labo-70th**（ブックマークしておくと便利です）
   - わからない時は、右上の自分のアイコン → **Your repositories** → `labo-70th` をクリック。
3. ファイルの一覧が出ます。主に使うのはこの2つだけです。
   - `index.html` … サイトの文章がすべて入っているファイル
   - `images` … 写真を入れるフォルダ

他のファイル（`style.css`、`script.js` など）は触らなくて大丈夫です。

---

## 2. 文章を変える

1. ファイル一覧の **`index.html`** をクリックします。
2. 右上の **鉛筆マーク（✏️ Edit）** をクリックすると、書き換えられる画面になります。
3. 直したい文章を探します。**Ctrl + F（Macは ⌘ + F）** で今の文章の一部を検索すると早いです。
4. 文章の近くには、こんな目印があります。

   ```
   <!-- ✏️ ここから編集できます：開催日時 -->
   <p class="date">2026.10.31<br>SAT</p>
   <!-- ✏️ ここまで -->
   ```

   - **✏️ ここから 〜 ✏️ ここまで** の間にある文字が、書き換えてよい部分です。
   - `<p class="date">` や `</p>` のような **`<` と `>` で囲まれた部分は消さない**でください。その間の文字だけを直します。
   - 文章の途中で改行したい時は、改行したい位置に `<br>` と書きます。
5. 直し終わったら、右上の緑のボタン **Commit changes...** をクリック。
6. 小さな画面が出るので、そのまま緑の **Commit changes** をクリック（メモ欄は空でも、「日時を修正」などと書いてもOK）。
7. **1〜2分待ってから**、サイトを開き直して確認します。スマホの場合は、ページを一度閉じて開き直すと確実です。

### よく直す場所と、検索する言葉

| 直したいもの | Ctrl+F で探す言葉 |
|---|---|
| ごあいさつの本文 | `ごあいさつ本文` |
| 開催日時・場所 | `開催日時・場所` |
| 出店者（Food & Drink） | `出店者リスト` |
| DJ・写真展・抽選会・グッズ | `DJ` `写真展` `抽選会` `Original Goods` |
| 駐車場の案内 | `駐車場の案内` |
| スタッフの名前・ひとこと | `スタッフ 1人分` |
| 年表の年号・説明文 | `年表 1枠目` など |
| 帯に流れる文字 | `帯に流れる文字` |
| Instagram / LINE のリンク | `SNSリンク` |
| 地図ボタンのリンク | `地図ボタン` |

---

## 3. 写真を差し替える

写真は **「決まった名前のファイルを、同じ名前で入れ直す」** だけです。`index.html` を触る必要はありません。

### 写真の名前（この名前どおりに付けます）

| 場所 | ファイル名 |
|---|---|
| いちばん上の大きな写真 | `hero.jpg` |
| 年表の写真（左から順に） | `history-01.jpg` `history-02.jpg` … `history-09.jpg` |
| スタッフの写真（上から順に） | `staff-01.jpg` `staff-02.jpg` … `staff-05.jpg` |
| LINEなどで共有した時に出る画像 | `ogp.jpg` |

- 名前は **半角の英数字**で、`.jpg` で終わるようにします（例：`staff-01.jpg`）。大文字・全角・スペースは使いません。
- スマホで撮った写真（.jpg / .jpeg / .HEIC）のうち、**.HEIC はそのままでは使えません**。iPhone の場合は「設定 → カメラ → フォーマット → 互換性優先」にすると .jpg で撮れます。
- 写真はそのままでも表示されますが、大きいと開くのが遅くなります。**LINEで自分あてに送ってから保存した写真**は自動的に小さくなるので、それを使うと軽くて安心です。
- 写真の色は、サイト側で自動的に青い色調（ビラと同じ）になります。写真そのものを加工する必要はありません。

### 手順

1. ファイル一覧の **`images`** フォルダをクリックします。
2. 右上の **Add file** → **Upload files** をクリックします。
3. 写真をドラッグ＆ドロップ（または「choose your files」から選択）します。
   **ファイル名が元と同じ**なら、自動的に上書きされます。
4. 下にある緑の **Commit changes** をクリック。
5. 1〜2分後にサイトを開き直して確認します。

### 写真の名前を変えるには（パソコン）

- Windows：ファイルを右クリック → 「名前の変更」
- Mac：ファイルを選んで Enter キー

---

## 4. 出店者・スタッフ・年表を1件増やす

`index.html` を開いて鉛筆マークで編集画面にし、**「1件分」をコピーして、そのすぐ下に貼り付け**ます。
コピーする範囲は、ファイルの中に目印で書いてあります。

### 4-1. 出店者（Food & Drink）を1店増やす

Ctrl+F で `出店者リスト` を探すと、こんな行が並んでいます。**1行が1店**です。

```
<div class="shop"><b>タイラーメン</b><span>タイラーメン</span></div>
<div class="shop"><b>MYS'COFFEE <em>@315mys_coffee</em></b><span>コーヒー、バスクチーズケーキ</span></div>
```

1. どれか1行を、行のはじめ（`<div`）から終わり（`</div>`）までコピーします。
2. 最後の店の行の下に、貼り付けます。
3. 店名・インスタのID・提供内容を書き換えます。
   - `<b>店名 <em>@インスタID</em></b>` … インスタがない店は `<em>@…</em>` ごと消してOK
   - `<span>提供内容</span>`
4. 緑のボタンで保存。

減らす時は、その1行をまるごと消します。

### 4-2. スタッフを1人増やす

Ctrl+F で `スタッフ 1人分 ここから` を探します。
**`▼▼▼ スタッフ 1人分 ここから ▼▼▼` の行から `▲▲▲ スタッフ 1人分 ここまで ▲▲▲` の行まで**が1人分です。

1. その範囲（▼の行〜▲の行まで、全部）をコピーします。
2. 最後の人の `▲▲▲ … ここまで ▲▲▲` の行の**すぐ下**に貼り付けます。
3. 貼り付けた中の、名前・ひとことを書き換えます。
   - 肩書きは全員 `Staff` のままにします（家族・免許の有無で書き分けません）。
4. 写真を出すには：`images` に `staff-06.jpg` のような名前で写真を入れてから、
   ```
   <span>PHOTO<br>スタッフ 1</span>
   ```
   の行を消して、代わりに
   ```
   <img src="images/staff-06.jpg" alt="スタッフの名前">
   ```
   と書きます（`staff-06.jpg` の部分は入れた写真の名前、`alt="…"` の中は名前）。
5. 緑のボタンで保存。

写真とテキストの左右は自動で交互になるので、人数が変わっても崩れません。

### 4-3. 年表を1枠増やす

Ctrl+F で `年表 1枠目 ここから` を探します。
**`▼▼▼ 年表 ○枠目 ここから ▼▼▼` の行から `▲▲▲ 年表 ○枠目 ここまで ▲▲▲` の行まで**が1枠分です。

1. 写真がある枠（1枠目など）を、▼の行〜▲の行まで全部コピーします。
2. 入れたい位置（年代の順になる場所）に貼り付けます。左から古い順に並びます。
3. 書き換える場所
   - `<p class="year">1956</p>` … 年号
   - `<img src="images/history-01.jpg" …>` … `history-01.jpg` を、入れた写真の名前に
   - `<p class="cap">…</p>` … 説明文
   - `style="--w:28"` … 枠の幅。数字が大きいほど広い枠（17〜32くらい）。写真が少ない年代は狭く、節目は広く
   - `style="aspect-ratio:624/883"` … 枠の縦横比（横/縦）。縦長の写真なら `3/4`、横長なら `4/3` くらい。写真はこの比率に自動で切り抜かれます
4. 緑のボタンで保存。

### 4-4. オレンジの「PHOTO WANTED」枠に写真を入れる

写真待ちの枠は、こう書かれています。

```
<div class="shot empty" style="aspect-ratio:3/4">
  <span>PHOTO<br>WANTED</span>
</div>
```

1. `images` に写真を入れます（例：`history-02.jpg`）。
2. `<span>PHOTO<br>WANTED</span>` の行の**下**に、1行足します。
   ```
   <img src="images/history-02.jpg" alt="写真の説明">
   ```
3. 緑のボタンで保存。これだけで、オレンジの枠が写真に変わります（`<span>…</span>` の行は残しておいて大丈夫です）。
4. 年号の横の `<span class="tag">仮</span>` は、年号が確定したら消します。

---

## 5. やってはいけないこと

- **`<` と `>` で囲まれた部分を消さない・書き換えない**（例：`<p class="cap">`、`</div>`）。文字だけを直します。
- **⚠️ の目印がある部分を触らない**。「⚠️ 編集しないでください」と書かれた場所は、動きやレイアウトの仕組みです。
- **ファイルの名前を変えない**（`index.html`、`style.css`、`script.js`、写真の名前）。
- **`style.css` と `script.js` を編集しない**。
- 写真の名前に **日本語・全角・スペース**を使わない。
- 見出し（`<h2 …>`）の文字を変える時は、`data-t="…"` の中と、その後ろの文字を **両方同じ**にする（同じ文字が2回書いてあります）。
- 半角の `"`（ダブルクォート）や `<` `>` を文章の中に書かない。どうしても必要な時は全角の「”」「＜」「＞」を使う。

---

## 6. 壊してしまった時の戻し方

GitHubは、保存するたびに**前の状態を全部覚えています**。壊しても必ず戻せるので、安心して編集してください。

### 直したばかりで、何を変えたか分かっている時

もう一度 `index.html` を編集して、変えた部分を元に戻し、保存します。

### 何が悪いか分からない時（前の状態に丸ごと戻す）

1. `index.html` を開き、右上の **History**（時計のマーク）をクリック。
2. 保存した記録が新しい順に並びます。**「うまく動いていた頃」の記録**をクリックします（日時が目安）。
3. 右上の **Browse files** をクリック。その時点のファイル一覧が出ます。
4. `index.html` をクリックして開き、右上の **Raw** の隣にある **コピーのマーク（Copy raw file）** をクリック。これで当時の中身が丸ごとコピーされます。
5. 上のパンくず（`labo-70th` の文字）をクリックして最新の画面に戻り、`index.html` → 鉛筆マークで編集画面へ。
6. 中身を **Ctrl + A（全選択）** → **Ctrl + V（貼り付け）** で丸ごと入れ替えます。
7. 緑の **Commit changes...** → **Commit changes** で保存。

写真を間違えて上書きした時も同じ要領で、`images` の中のファイルの History から戻せます。

どうしても直らない時は、制作担当に「戻してほしい」と連絡してください。GitHub の記録から復元できます。

---

## 7. 感謝祭のあと：HISTORYの最後の枠に当日の写真を入れる

年表のいちばん右の枠（**2026.10.31**）は、**当日の写真を入れるために空けてあります**。
ここに写真が入ると、「これから開く感謝祭のお知らせ」だったこのサイトが、「70年目の一日の記録」に変わります。
このサイトはその時に完成します。

### 手順

1. 当日の写真を1枚選び、名前を **`history-09.jpg`** にします（横長の写真がきれいに収まります）。
2. `images` フォルダ → **Add file → Upload files** で `history-09.jpg` を入れて、緑のボタンで保存。
3. `index.html` → 鉛筆マーク → Ctrl+F で `年表 最後の枠` を探します。こう書かれています。

   ```
   <div class="shot empty" style="aspect-ratio:16/10">
     <span>この枠は当日の写真用に<br>空けておきます</span>
   </div>
   ```

4. `<span>…</span>` の行の**下**に、この1行を足します。

   ```
   <img src="images/history-09.jpg" alt="70周年感謝祭の一日">
   ```

5. すぐ下の説明文 `<p class="cap">…</p>` の中を、その日のことばに書き換えます。
   （例：「2026年10月31日。たくさんの方に来ていただきました。ありがとうございました。」）
6. 緑の **Commit changes...** → **Commit changes** で保存。1〜2分後にサイトで確認します。

写真を入れると、他の写真と同じ青い色調になり、パソコンでは写真の上でマウスを動かすと丸い鏡の中だけ元の色が戻ります。スマホでは指でなぞると同じことが起きます。

カウントダウンは、開催時刻を過ぎると自動的に「終了しました」の文に切り替わります。この文も `index.html` の `終了しました` を検索すれば書き換えられます。

---

### 困った時の連絡先

制作担当：（ここに連絡先を書く）
